# === Dane użytkowników (runtime) ===
data/
trash/

# === Pliki tymczasowe ===
tmp/
temp/
*.bak
*.tmp
*.log
NDH6SA~M

# === System / IDE ===
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# === Composer / npm (na przyszłość) ===
vendor/
node_modules/

# === Narzędzia lokalne ===
browser.php

# === Env ===
.env
.env.local

# === API tokeny (sekret bearer, web-denied, ręcznie edytowane) ===
inc/api-tokens.txt

# === Źródłowe wersje grafik (duże PNG, nie produkcyjne) ===
assets/art/_src/

# === Druga linia obrony: prywatne katalogi przestrzeni roboczej ===
# Korzeniem repo jest folder www/. Poniższe ścieżki leżą PIĘTRO WYŻEJ, poza
# katalogiem repo, więc git ich nie widzi niezależnie od tego pliku - granicę
# wymusza sama struktura katalogów, nie lista wyjątków.
# Wpisy zostają celowo: jeśli którykolwiek z tych katalogów kiedykolwiek trafi
# do wnętrza www/, ma zostać zignorowany, a nie opublikowany. Repo jest publiczne.
docs/
security-audit/
interop/
_new_design*/
CLAUDE.md
.claude/
